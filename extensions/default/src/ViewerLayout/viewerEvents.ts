/**
 * Fired on `window` when a series is picked from the study browser with a
 * single tap (small screens), so the layout can close the panel once the
 * series is in the viewport.
 */
export const SERIES_SELECTED_EVENT = 'ohif:viewer:series-selected';
